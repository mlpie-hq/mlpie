apiVersion: mlpie.ai/v1alpha1
kind: Pipeline
metadata:
  name: invalid-pipeline
spec:
  description: "This pipeline is intentionally invalid (missing engine field)"
  project: project1
  environment: dev
  # engine field is intentionally missing
  input:
    - dataset: customers
  output:
    dataset: customers_processed
  steps:
    - name: clean
      type: transform
      config:
        script: "clean_data.py" 